if(blocks[1]){ const el = blocks[1].querySelector('.text-[11px]'); if(el) el.textContent = 'Ocupação'; }
          if(blocks[3]){ const el = blocks[3].querySelector('.text-[11px]'); if(el) el.textContent = 'Faturação'; }
          const prev=document.getElementById('btnPrevMonth'); if(prev) prev.tit
